import SwiftUI

struct PlaneOverviewView: View {
    var body: some View {
        VStack {
            Text("Plane Overview")
                .font(.largeTitle)
                .padding()
            
            Text("Your focus stats and upgrades will live here.")
                .padding()
        }
    }
}

